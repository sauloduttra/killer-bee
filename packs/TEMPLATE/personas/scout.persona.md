---
name: scout
display_name: Scout
description: Replace me — one sentence the catalog and the app both show.
# model is optional; format is "provider:model-id" (split at the first ':').
# Omit it to let the importing user's app defaults decide.
---
You are Scout. Replace this body with the system prompt — the body of this
file IS the prompt, verbatim, and the catalog publishes it in full. Write it
knowing it will be read line by line before anyone runs it: that is the
product.

Say what the agent does, what it refuses, and how it formats answers. Keep
every capability claim honest — the reviewer will check this text against
what the pack promises.
