# Spec vs implementação — `PERSONA_PACK_SPEC.md` contra o código

**Commit:** `ed4b3e7afafb` · **Gerado por** `scripts/evidence_to_docs.py` a partir
de [`evidence/spec_rows.json`](evidence/spec_rows.json) — edite lá, não aqui.

O documento que ninguém tem: uma linha por funcionalidade que o spec descreve,
quatro estados, citação dos dois lados. Um mantenedor lê isto e vê em cinco
minutos onde a própria casa está desalinhada.

| estado | linhas |
|---|---|
| ✅ documentado E implementado | 43 |
| ≠ documentado DIVERGENTE do implementado | 25 |
| ⛔ documentado e AUSENTE | 21 |
| 🗓 documentado como futuro (e de fato ausente) | 2 |
| [+] implementado e NÃO documentado (direção reversa) | 13 |


## ⛔ documentado e AUSENTE (21)

| id | funcionalidade | evidência | nota |
|---|---|---|---|
| f-11 | Prohibited pack contents (working-dir state, secrets, build artifacts) enforced by tooling | `crates/buzz-persona/src/validate.rs:155-162` · `crates/buzz-persona/src/pack.rs:117-124` | The spec rule is normative guidance for authors only. No code anywhere (buzz-persona validator, loader, CLI, desktop) scans pack contents for working-dir state, secrets, or build artifacts. Adjacent-but-different protections exist: path traversal rejection an… |
| f-32 | Skill collision handling: existing skill not overwritten; buzz-acp must log WARN | `crates/buzz-persona/src/pack.rs:243-249` · `crates/buzz-persona/PERSONA_PACK_SPEC.md:1146` | No skill copying to agent working directories exists anywhere, so collision handling and the WARN cannot exist either. buzz-persona's resolve_skills() only computes a persona→skill-name map. The spec's own PF-3 marks this as planned. |
| f-44 | Hooks are entirely a harness (buzz-acp) feature; agent runtimes have no hook system | `crates/buzz-persona/src/resolve.rs:57` · `crates/buzz-persona/src/persona.rs:82-93` | The claim that hooks are implemented in buzz-acp is not true of the code: buzz-acp contains zero hook logic. Hooks exist only as parsed data in buzz-persona (frontmatter Hooks struct, HooksData, ResolvedHooks) and are never fired. The spec's Section 9 impleme… |
| f-45 | hooks/hooks.json pack-level hook file (on_start/on_stop/on_message) applying to all agents | `crates/buzz-persona/src/manifest.rs:114-116` · `crates/buzz-persona/src/pack.rs:111-112` | Only the manifest path field is parsed and then deliberately dropped by the loader; no parser or consumer for the pack-level hooks.json file format exists anywhere (buzz-acp, desktop, or buzz-persona). Per-persona frontmatter hooks parse (see f-17) but are al… |
| f-46 | Per-persona hooks override pack-level hooks for that agent | `crates/buzz-persona/src/pack.rs:111-112` · `crates/buzz-persona/src/resolve.rs:57` | Per-persona hooks ARE parsed from frontmatter (persona.rs Hooks struct with deny_unknown_fields) and carried into ResolvedHooks, but pack-level hooks.json is never read anywhere (manifest field is parsed then deliberately dropped by the pack loader), so there… |
| f-47 | Hook points fired: on_start before session, on_stop after session ends, on_message before each dispatch | `crates/buzz-persona/src/persona.rs:84-93` · `crates/buzz-persona/src/resolve.rs:76-82` | The three hook point NAMES exist only as parsed config fields; none of the firing semantics (timing, stdin contract, timeout, exit-code handling, abort-on-on_start-failure) is implemented anywhere. Consistent with the spec's own Section 9 implementation note,… |
| f-48 | Hook execution environment (buzz-acp user, cwd=$AGENT_CWD, agent env vars) and exit-code semantics | `crates/buzz-persona/src/resolve.rs:343-347` · `crates/buzz-persona/PERSONA_PACK_SPEC.md:510-511` | No hook execution exists, so no execution environment or exit-code semantics exist. resolve.rs explicitly stores hook paths raw and defers even path-traversal validation to 'the PR that wires execution'. |
| f-49 | on_message hook contract: stdin UTF-8 input, read-only, 5s timeout with SIGKILL, output logged at DEBUG | `crates/buzz-persona/src/resolve.rs:57-58` · `crates/buzz-persona/src/resolve.rs:343-346` | Hooks never execute anywhere in the repo, so no part of the on_message contract (stdin delivery, message-drop on non-zero exit, 5s SIGKILL, DEBUG logging) exists. |
| f-50 | on_stop crash caveat: does not fire on buzz-acp crash (SIGSEGV, OOM) | `crates/buzz-persona/src/resolve.rs:340-346` · `crates/buzz-persona/PERSONA_PACK_SPEC.md:510-511` | The caveat describes runtime behavior of hook execution, and hook execution does not exist anywhere in the repo (hooks parsed only). The spec's own implementation note concedes this. Nothing to diverge from — the documented on_stop lifecycle is entirely unimp… |
| f-65 | .buzzpack archive format: pack distributed as zip file (Phase 1) | `crates/buzz-cli/src/lib.rs:1780-1791` · `crates/buzz-cli/src/lib.rs:2272` | The CLI has only `buzz pack validate` and `buzz pack inspect` operating on directories. No archive builder, no installer, no pack.lock, no ~/.buzz/packs install location anywhere in the repo. |
| f-67 | buzz pack packaging command with --output producing .buzzpack archives | `crates/buzz-cli/src/lib.rs:1780-1793` · `crates/buzz-cli/src/commands/pack.rs:1-3` | No packaging, no .buzzpack format, no sha256 checksum generation/verification, and no `buzz install` exist anywhere in the repo. Only `buzz pack validate` and `buzz pack inspect` are implemented. |
| f-68 | buzz install command accepting local .buzzpack path or HTTPS URL | `crates/buzz-cli/src/lib.rs:2272` · `crates/buzz-cli/src/lib.rs:2082-2105` | The CLI's own inventory-stability tests pin the command groups: no 'install' group exists and the 'pack' group contains exactly inspect and validate. No packaging (.buzzpack), checksum verification, or HTTP-install code exists anywhere. |
| f-69 | Required .buzzpack.sha256 checksum: verify before install, refuse on mismatch, fetch from same base URL | `crates/buzz-cli/src/commands/pack.rs:1-3` | No pack install pipeline exists at all (see f-65), so no checksum generation, verification, or same-base-URL fetch exists either. |
| f-70 | pack.lock Phase 1 format recording source, sha256, version, installed_at per pack id | — | No install flow, no .buzzpack zip handling, no sha256 verification, no lockfile reader/writer anywhere in crates/, desktop/, or scripts. The pack loader also never looks for a pack.lock file. |
| f-71 | Phase 2 git install syntaxes: github: shorthand, @tag pinning, git+https URLs | `crates/buzz-cli/src/lib.rs:1780-1793` | Spec labels this Phase 2; consistent with that, no install machinery of any kind (zip or git) exists in the repo. |
| f-72 | pack.lock for git installs records resolved commit SHA; verified on install | `crates/buzz-persona/PERSONA_PACK_SPEC.md:887` · `crates/buzz-cli/src/lib.rs:2272` | There is no install pipeline at all (Phase 1 or Phase 2), so no lockfile, no git resolution, and no verification exist. pack.lock appears only in the spec document. |
| f-74 | Installed pack location ~/.buzz/packs/<pack-id>/, read by buzz-acp at agent startup | — | The closest analog is the desktop's installed-team directory (app-data agents/teams/<team-id>/ holding *.persona.md and instructions.md), which is a different location and consumed by the desktop app, not buzz-acp. |
| f-85 | Hook security: hooks run with buzz-acp privileges; display hook commands before first execution (Phase 2) | `crates/buzz-persona/src/resolve.rs:343-346` | Hooks are never executed, so the privileges concern is not yet exercisable and the Phase-2 'display hook commands before first execution' feature does not exist. The spec itself frames the display as a Phase 2 feature; hook execution itself is also marked pla… |
| f-86 | V6 (buzz-namespaced) to current format migration field mapping | `crates/buzz-persona/src/persona.rs:174-175` | The mapping table is documentation-only guidance for manual migration; no code implements it. Consistent with the spec's own line 1104 ('V6 namespaced buzz: block format is not supported'): deny_unknown_fields on the persona frontmatter parser makes a V6 `buz… |
| f-87 | Pre-V6 flat JSON persona to .persona.md migration mapping (system_prompt to markdown body, channels to subscribe) | `desktop/src-tauri/src/commands/personas/snapshot/import.rs:33-34` · `desktop/src-tauri/src/commands/media_download.rs:508` | The mapping is manual-migration guidance only. No conversion tooling exists anywhere; the desktop actively REJECTS legacy .persona.json (and .persona.md/.zip) imports rather than migrating them. (The desktop's migration.rs handles a different, unrelated migra… |
| f-89 | Multi-pack skill name collision: first-installed pack wins silently under the no-overwrite rule | `crates/buzz-persona/src/pack.rs:243-249` | Neither pack installation nor skill copying exists, so the no-overwrite rule and its first-installed-wins consequence have no implementation anywhere. buzz-agent's discovery-time dedup (hints.rs 136-139: first directory scanned wins by name across .agents/.go… |

## ≠ documentado DIVERGENTE do implementado (25)

| id | funcionalidade | evidência | nota |
|---|---|---|---|
| f-04 | engines.buzz minimum-version negotiation; buzz-acp rejects packs requiring newer version | `crates/buzz-persona/src/manifest.rs:37-41` · `crates/buzz-persona/src/manifest.rs:180` | The engines.buzz field is parsed and stored (and whitelisted by the validator), but no code anywhere compares it to a Buzz version or rejects a pack. There is also no pack install path in buzz-acp at all (see f-68), so the documented rejection point does not … |
| f-08 | hooks_config manifest field pointing to hooks file | `crates/buzz-persona/src/manifest.rs:114-116` · `crates/buzz-persona/src/pack.rs:111-112` | The manifest field is accepted, deserialized, and known to the validator, but the referenced hooks file is never read by any code: pack.rs deliberately drops the field and buzz-acp contains no hooks logic. Only per-persona frontmatter `hooks:` are parsed (per… |
| f-10 | Pack directory layout and conventions (agents/ flat, skills/ subdirs, .plugin/, optional hooks/, instructions.md, .mcp.… | `crates/buzz-persona/src/pack.rs:132-135` · `crates/buzz-persona/src/pack.rs:5-15` | Implemented: .plugin/plugin.json required; skills/ one-subdir-per-skill enumerated; instructions.md and .mcp.json optional with root-default fallback; pack.lock ignored harmlessly. Divergent: 'agents/ flat, no nesting' is not enforced — persona files load fro… |
| f-13 | Identity fields: name/display_name/description required; avatar, version (defaults to pack version), author optional | `crates/buzz-persona/src/persona.rs:220-237` · `crates/buzz-persona/src/persona.rs:108-119` | Required fields (name/display_name/description) enforced including non-empty checks; avatar/version/author optional — matches. Divergence: frontmatter `version` is parsed (persona.rs 116) but dropped when building LoadedPersona (pack.rs 77-98 has no version f… |
| f-16 | Behavioral config field group in frontmatter (subscribe, triggers, model, temperature, max_context_tokens, thread_repli… | `crates/buzz-persona/src/persona.rs:129-165` · `crates/buzz-acp/src/filter.rs:1-6` | All seven fields are parsed and precedence-merged in buzz-persona and shown by `buzz pack inspect`, but buzz-acp never consumes them: no buzz_persona usage exists in buzz-acp code (dependency declared in Cargo.toml only), and its subscription/dispatch uses it… |
| f-20 | Two-layer prompt architecture with fixed section order ([Base], [System], [Context], [Thread/Conversation Context], [Bu… | `crates/buzz-acp/src/queue.rs:1393-1399` · `crates/buzz-acp/src/queue.rs:1434-1447` | Section order exists in format_prompt() but diverges: (1) [Base]/[System] ride in the user message only for legacy agents (protocol_version < 2); modern agents get them via systemPrompt in session/new (pool.rs framed_system_prompt, lines 1262-1283); (2) team … |
| f-28 | $AGENT_CWD resolution order: AGENT_CWD env var, then current_dir(), else refuse to start; stored as session.working_dir | `crates/buzz-acp/src/lib.rs:1599-1602` · `crates/buzz-acp/src/pool.rs:1287-1289` | Divergence on all three steps: no AGENT_CWD env var exists anywhere in code (step 1 absent); current_dir() is used but on failure buzz-acp silently falls back to "/" instead of refusing to start (step 3 contradicted); the value is passed as session/new cwd. `… |
| f-29 | Skill load key is the name: field from SKILL.md frontmatter | `crates/buzz-agent/src/hints.rs:86-108` · `crates/buzz-agent/src/builtin.rs:57` | Core claim holds: the load key is the SKILL.md frontmatter `name:` (required, non-empty; skill silently skipped when frontmatter fails, hints.rs 128-135, with no directory-name fallback). Divergences from the cited spec text: invocation is the built-in `load_… |
| f-30 | SKILL.md name: and description: required; missing/malformed frontmatter silently skips skill with no directory-name fal… | `crates/buzz-agent/src/hints.rs:94-99` · `crates/buzz-agent/src/hints.rs:100-105` | Matches spec: malformed/missing frontmatter or missing/empty name → skill silently skipped (else continue), and there is no fallback to the directory name. Diverges: description is NOT required — parse_skill_frontmatter defaults it to "" via unwrap_or(""), so… |
| f-31 | Skill scoping: claimed skill copied only to that persona; unclaimed skills copied to all agents | `crates/buzz-persona/src/pack.rs:243-248` · `crates/buzz-persona/src/pack.rs:294-315` | Divergence: the scoping ALGORITHM (claimed-only vs unclaimed-shared, with path normalization) is fully implemented in buzz_persona::pack::resolve_skills and unit-tested, but the COPY the spec describes never happens — the function has no non-test callers and … |
| f-33 | Skills not auto-loaded; explicit load call; available skills listed in user message prefix by buzz-acp | `crates/buzz-agent/src/builtin.rs:3-5` · `crates/buzz-agent/src/hints.rs:239-247` | On-demand loading with an explicit call is implemented — but in the buzz-agent runtime: skills are listed under '## Available Skills' in the runtime's own system prompt (built by hints.rs from $AGENT_CWD/.agents|.goose|.claude/skills) with the `load_skill` to… |
| f-36 | MCP transport restriction: only stdio and streamable_http; SSE rejected causing session startup failure | `crates/buzz-acp/src/acp.rs:23-33` · `crates/buzz-agent/src/lib.rs:302` | In-repo reality is stricter and different: buzz-acp's McpServer struct is stdio-only (no transport/type field), and the in-repo buzz-agent runtime advertises http:false, sse:false. streamable_http support and the quoted SSE-rejection error exist nowhere in th… |
| f-38 | MCP merge rules: pack-level base set; name collision means per-persona entry wins entirely; merged set passed via NewSe… | `crates/buzz-persona/src/resolve.rs:270-275` · `crates/buzz-persona/src/resolve.rs:295-300` | Merge rules 1-2 are implemented and tested in buzz_persona::resolve::merge_mcp_servers (pack .mcp.json base, persona entry replaces whole server on name collision, no partial merge, ${VAR} literals preserved). Rule 3 diverges: buzz-acp does send an mcpServers… |
| f-41 | instructions.md appended to persona prompt in user message prefix; no file written to disk | `crates/buzz-acp/src/queue.rs:1441-1447` · `crates/buzz-acp/src/pool.rs:1304-1317` | No file is written to the agent workspace — instructions flow desktop env var → config.team_instructions (config.rs 1070-1075) → in-memory prompt assembly. Divergences: rendered as a separate '[Team Instructions]' section after [System], not appended inside t… |
| f-43 | Hooks parsed and validated at pack load time but not yet executed | `crates/buzz-persona/src/persona.rs:82-93` · `crates/buzz-persona/src/resolve.rs:57` | Per-persona frontmatter hooks match the spec claim exactly: parsed with deny_unknown_fields (typo like on_init is a hard error, test unknown_hook_key_errors persona.rs:425-430), stored as raw paths, never executed. Divergence: PACK-LEVEL hooks/hooks.json (spe… |
| f-52 | Five-level configuration precedence model (operator env > desktop UI > persona frontmatter > pack defaults > built-in) | `crates/buzz-persona/src/merge.rs:1-8` · `crates/buzz-acp/src/acp.rs:503-510` | Levels 3–5 are implemented in buzz-persona (merge.rs/resolve.rs) and level 1 in AcpClient::spawn (parent env wins over extra_env). Level 2 (desktop UI per-agent overriding pack values) is not wired: the desktop's own precedence model is Definition > Global > … |
| f-59 | Canonical behavioral config field schema with built-in defaults, identical for defaults object and persona frontmatter | `crates/buzz-persona/src/manifest.rs:43-70` · `crates/buzz-persona/src/merge.rs:177-198` | Same-schema-both-places holds: BehavioralDefaults mirrors the persona behavioral fields (model, temperature, max_context_tokens, subscribe, triggers+respond_to alias, thread_replies, broadcast_replies), and both feed the same merge. Built-ins match the spec t… |
| f-61 | Unknown keys: validation warning in defaults, hard error in persona frontmatter, WARN-and-ignore at deploy | `crates/buzz-persona/src/validate.rs:317-331` · `crates/buzz-persona/src/persona.rs:173-175` | Two of three legs match: `buzz pack validate` warns on unknown top-level/defaults/triggers keys (catches 'temprature'), and persona frontmatter uses deny_unknown_fields hard errors (test persona.rs 415-422; surfaced as validate errors, validate.rs 730-753). D… |
| f-62 | Channel # prefix is display-only convention; stripped before relay API calls | `crates/buzz-core/src/channel.rs:15-18` · `crates/buzz-sdk/src/builders.rs:682` | The convention itself is implemented — '#' is stripped by buzz_core::channel::canonical_channel_name applied in SDK event builders, relay ingestion, DB layer, and desktop (canonicalChannelName.ts). Divergence: the spec attributes the stripping to buzz-acp bef… |
| f-79 | Delivery summary: skill copy is the only filesystem write; other components via protocol, env vars, or user-message pre… | `crates/buzz-acp/src/pool.rs:916-927` · `crates/buzz-acp/src/queue.rs:1363-1365` | Three of the table's rows hold: MCP servers go via session/new (protocol, no .mcp.json written), model/provider via child-process env vars, behavioral config consumed by harness dispatch. Divergences: (1) the skill copy — the table's single '✅ Yes' filesystem… |
| f-80 | [System] block prepended to user message text by format_prompt() in queue.rs; NOT injected into actual system prompt | `crates/buzz-acp/src/queue.rs:1434-1440` · `crates/buzz-acp/src/acp.rs:650-652` | The documented current state is now only the legacy fallback: format_prompt() prepends [System] to the user message solely for agents with protocol_version < 2. For modern agents the persona prompt IS injected into the actual system prompt via session/new sys… |
| f-81 | [System] block contents: persona prompt, Team Instructions, available-skills list with load() usage line | `crates/buzz-acp/src/queue.rs:1438-1440` · `crates/buzz-acp/src/queue.rs:1441-1447` | The [System] block carries the persona prompt (user-message prefix for legacy agents; session/new systemPrompt for protocol v2 — spec Section 12 documents only the user-message path and PF-1 marks system-role injection as planned, but it exists). Team instruc… |
| f-82 | True system prompt injection planned (PF-1): extend_system_prompt() once at session creation instead of per-turn prefix | `crates/buzz-acp/src/acp.rs:649-651` · `crates/buzz-acp/src/pool.rs:894-896` | Spec says once-at-creation injection is planned via agent.extend_system_prompt() and that the per-turn [System] prefix is current behavior. Reality: once-at-creation injection is already implemented, but via different mechanisms — bare systemPrompt field in s… |
| f-90 | Open questions and future work (session/set_model, CONTEXT_FILE_NAMES, skill versioning, pack signing, multi-pack confl… | `crates/buzz-agent/src/lib.rs:247-248` · `crates/buzz-acp/src/pool.rs:1086-1088` | Divergent because open question 1 is no longer open: session/set_model is fully implemented — buzz-agent handles the method (per-session model override) and buzz-acp calls it after session/new to apply --model/BUZZ_ACP_MODEL (ModelSwitchMethod::SetModel), exa… |
| f-91 | Planned features table PF-1 through PF-6, with PF-2 and PF-6 marked Implemented | `crates/buzz-persona/src/validate.rs:143-164` · `crates/buzz-cli/src/commands/pack.rs:24` | Verified accurate: PF-2 (buzz pack validate CLI wired end-to-end: schema/identity/behavioral checks, unknown-key warnings, skill-name-mismatch warnings) and PF-6 (AcpClient::spawn extra_env via Command::env with std::env::var_os operator-precedence check). Al… |

## 🗓 documentado como futuro (e de fato ausente) (2)

| id | funcionalidade | evidência | nota |
|---|---|---|---|
| f-25 | Skill path resolution and runtime copying to .agents/skills/ is planned, not yet implemented | `crates/buzz-persona/src/resolve.rs:60-61` · `crates/buzz-persona/src/pack.rs:243-249` | Matches the spec's own claim: buzz-persona computes the per-persona skill-name mapping (resolve_skills, pack.rs 249-315 — pure, no filesystem writes) and stores declared paths, but nothing anywhere copies pack skills into an agent working directory. |
| f-73 | Phase 3 App Store UI: Buzz-hosted registry, OPS-API-compatible (details TBD) | `desktop/src-tauri/src/managed_agents/types/catalog_source.rs:6-11` | Consistent with the spec: Phase 3 is explicitly TBD and nothing resembling a pack registry or in-app pack browser is implemented. |

## ✅ documentado E implementado (43)

| id | funcionalidade | evidência | nota |
|---|---|---|---|
| f-01 | Persona Pack is a superset of OPS; OPS consumers safely ignore unknown fields | `crates/buzz-persona/src/manifest.rs:126-129` · `crates/buzz-persona/src/manifest.rs:79-121` | Buzz's side of the contract is implemented: OPS metadata fields sit at top level alongside Buzz extensions, and the parser (RawManifest, no deny_unknown_fields) silently tolerates foreign/unknown fields; validate emits only advisory warnings and accepts OPS-s… |
| f-02 | .plugin/plugin.json manifest following OPS schema with Buzz extensions at top level | `crates/buzz-persona/src/pack.rs:132` · `crates/buzz-persona/src/manifest.rs:126-129` | PackManifest carries OPS fields (id/name/version/license/homepage/keywords/engines) plus top-level Buzz fields (personas, defaults, pack_instructions, mcp_config, hooks_config). Unknown fields tolerated (no deny_unknown_fields on RawManifest); validator warns… |
| f-03 | OPS metadata manifest fields ($schema, id, name, version, description, author, license, homepage, keywords) parsed from… | `crates/buzz-persona/src/manifest.rs:79-121` · `crates/buzz-persona/src/manifest.rs:87-97` | PackManifest parses id/name/version (required, non-empty enforced) plus description, author, license, homepage, keywords, engines. $schema is not stored as a typed field but the permissive RawManifest (no deny_unknown_fields, manifest.rs:126-129) accepts it a… |
| f-05 | personas manifest field is the authoritative list of persona file paths | `crates/buzz-persona/src/manifest.rs:102-104` · `crates/buzz-persona/src/pack.rs:158-167` | load_pack reads exactly the files listed in `personas` (each safe_resolve'd against the pack root, missing file = PersonaNotFound error); no directory scanning of agents/ occurs, so the list is authoritative. Zero personas is a hard error at resolve and valid… |
| f-06 | pack_instructions manifest field pointing to instructions file | `crates/buzz-persona/src/manifest.rs:106-108` · `crates/buzz-persona/src/pack.rs:173-182` | Field parsed, referenced file read (bounded) with hard error when path missing; falls back to instructions.md in pack root when field absent (pack.rs:184-191). Content flows into ResolvedPersona.pack_instructions. |
| f-07 | mcp_config manifest field pointing to pack-level MCP config file | `crates/buzz-persona/src/manifest.rs:110-112` · `crates/buzz-persona/src/pack.rs:201-211` | load_pack resolves the mcp_config path (safe_resolve, size-bounded read, JSON parse with McpConfigParse error) and falls back to .mcp.json at the pack root when the manifest field is omitted. |
| f-09 | defaults manifest object provides pack-wide behavioral defaults resolved before per-persona configurations | `crates/buzz-persona/src/manifest.rs:43-70` · `crates/buzz-persona/src/pack.rs:405-410` | Defaults semantics (levels 3-5 precedence) are fully implemented in buzz-persona and applied at pack-load time. Caveat on the second citation: buzz-acp itself never loads packs — grep 'buzz_persona' in crates/buzz-acp/src yields 0 hits (the Cargo.toml depende… |
| f-12 | .persona.md file format: YAML frontmatter + markdown body | `crates/buzz-persona/src/persona.rs:2-4` · `crates/buzz-persona/src/persona.rs:208-218` | Full implementation with size limits (1 MiB frontmatter / 256 KiB body), required identity fields, deny_unknown_fields, and the body stored as PersonaConfig::prompt. Extensive tests in the same file. |
| f-14 | Persona skills frontmatter field (pack-relative skill directory paths) | `crates/buzz-persona/src/persona.rs:121-123` · `crates/buzz-persona/src/pack.rs:243-248` | Frontmatter `skills:` parsed as pack-relative paths (doc comment: 'Pack-relative paths to skill directories'); resolve_skills implements the 'this agent only' scoping (claimed skills go only to the claiming persona; unclaimed shared to all), with path normali… |
| f-15 | Persona mcp_servers frontmatter field with name/command/args/env entries | `crates/buzz-persona/src/persona.rs:68-79` · `crates/buzz-persona/src/persona.rs:125-127` | McpServerConfig has exactly name (required), command (required), args (default []), env (default {}). Per-persona entries are merged over pack-level .mcp.json in merge_mcp_servers with persona-wins-entirely collision semantics, as the spec's Section 7 merge r… |
| f-17 | Persona hooks frontmatter field (harness-managed lifecycle hooks) | `crates/buzz-persona/src/persona.rs:81-93` · `crates/buzz-persona/src/persona.rs:164-165` | The frontmatter field exists with on_start/on_stop/on_message and deny_unknown_fields (typo 'on_init' is a parse error, persona.rs 424-430), and is carried through pack.rs 419-424 into ResolvedHooks (resolve.rs 347-357, paths kept raw pending safe_resolve). E… |
| f-18 | respond_to accepted as legacy alias for triggers (frontmatter and plugin.json defaults) | `crates/buzz-persona/src/persona.rs:189-190` · `crates/buzz-persona/src/manifest.rs:62-63` | Alias present on both the persona Frontmatter struct and BehavioralDefaults in the manifest; validator also checks defaults.respond_to (validate.rs:222-229). Tested in both parsers (manifest.rs:278-288). |
| f-19 | Markdown body after closing --- is the persona prompt, embedded directly | `crates/buzz-persona/src/persona.rs:3-4` · `crates/buzz-persona/src/persona.rs:277-319` | Body after the closing --- (own-line delimiter enforced; '---junk' rejected) becomes PersonaConfig::prompt and flows verbatim into ResolvedPersona.system_prompt. No external-file dereferencing exists anywhere in the prompt path, matching 'embed directly'. Bod… |
| f-21 | [Base] layer compiled into buzz-acp, identical for every agent (platform identity, tool reference, workspace layout, po… | `crates/buzz-acp/src/lib.rs:1591-1597` · `crates/buzz-acp/src/base_prompt.md:1` | Compiled-in static string shared by all agents in the process; framed via base_section() (queue.rs 1387-1389) for legacy agents and framed_system_prompt() (pool.rs 1262-1283) for session/new delivery. Covers platform identity, workspace layout, and polling; t… |
| f-22 | BUZZ_ACP_NO_BASE_PROMPT env var omits the [Base] section | `crates/buzz-acp/src/config.rs:402-405` · `crates/buzz-acp/src/lib.rs:1591-1597` | Flag/env sets ctx.base_prompt to None, omitting [Base] from both delivery paths (system-role at session/new for protocol v2 and user-message section for legacy agents). Companion BUZZ_ACP_BASE_PROMPT_FILE (config.rs:407-414) replaces the compiled-in default, … |
| f-23 | BUZZ_ACP_BASE_PROMPT_FILE env var replaces compiled-in base prompt with custom file read at startup | `crates/buzz-acp/src/config.rs:407-414` · `crates/buzz-acp/src/config.rs:873-887` | Clap arg --base-prompt-file / BUZZ_ACP_BASE_PROMPT_FILE, mutually exclusive with BUZZ_ACP_NO_BASE_PROMPT (conflicts_with), read at startup with a 1 MB cap; when present it replaces the compiled-in base_prompt.md via Box::leak; no_base_prompt yields None. |
| f-24 | [System] layer: persona prompt, unique per agent | `crates/buzz-acp/src/pool.rs:1273` · `crates/buzz-acp/src/queue.rs:1438-1439` | Each buzz-acp process carries one per-agent system prompt (--system-prompt/--system-prompt-file), rendered under the [System] header distinct from [Base]. Delivery differs by protocol version (see f-20/f-80), but the layer itself — per-agent persona prompt se… |
| f-26 | Skills: reusable on-demand instruction sets in markdown | `crates/buzz-agent/src/hints.rs:8` · `crates/buzz-agent/src/hints.rs:240-246` | In-repo agent runtime (buzz-agent) discovers SKILL.md files from the spec's three directories, lists name+description in the prompt, and lazily loads the body via the load_skill tool (frontmatter stripped, supporting files enumerable). Divergence detail only … |
| f-27 | Skill discovery under $AGENT_CWD with .agents/skills canonical; pack-root skills/ NOT scanned | `crates/buzz-agent/src/hints.rs:8` · `crates/buzz-agent/src/hints.rs:204-217` | Discovery lives in the in-repo buzz-agent runtime, keyed off session/new cwd (must be absolute, lib.rs:343-350) — matching the $AGENT_CWD definition. SKILL_DIRS contains exactly the three spec paths; bare 'skills' is absent so pack-root skills/ is never scann… |
| f-34 | SKILL.md file format (frontmatter + content); buzz pack validate does not yet enforce required metadata fields | `crates/buzz-persona/src/validate.rs:426-429` · `crates/buzz-persona/src/validate.rs:402-405` | Exactly as documented: advisory_check_skill_names warns when SKILL.md name differs from its directory name (tested at validate.rs:757-786), but a SKILL.md with malformed/missing frontmatter or missing name/description passes validation silently (every failure… |
| f-35 | Two-level MCP configuration: pack-level .mcp.json shared plus per-persona entries merged | `crates/buzz-persona/src/pack.rs:201-222` · `crates/buzz-persona/src/resolve.rs:283-299` | Pack-level .mcp.json (mcpServers map) is the base set; persona frontmatter mcp_servers list merges on top with persona winning entirely on name collision; output sorted deterministically. Matches spec Section 7 merge rules 1-2. Downstream, buzz-acp passes MCP… |
| f-37 | Pack-level .mcp.json format with mcpServers object (command/args/env) | `crates/buzz-persona/src/pack.rs:201-222` · `crates/buzz-persona/src/resolve.rs:283-293` | Loads .mcp.json from the manifest's mcp_config path or the pack-root default, parses the documented mcpServers name→{command,args,env} shape, merges with per-persona frontmatter servers (persona wins on name collision, resolve.rs 295-300), and passes ${VAR} e… |
| f-39 | ${VAR_NAME} MCP env interpolation not implemented; values passed through as literals | `crates/buzz-persona/src/resolve.rs:276` · `crates/buzz-persona/src/resolve.rs:494-506` | Code matches the spec's documented CURRENT behavior exactly: literals preserved end-to-end, verified by test mcp_env_literals_preserved. The startup-error-on-unresolved-vars behavior is explicitly future-tense in the spec ('When implemented') and does not exi… |
| f-40 | MCP delivery: no .mcp.json written to agent working directory | `crates/buzz-acp/src/acp.rs:645-648` · `crates/buzz-persona/src/pack.rs:213-218` | MCP servers reach the agent exclusively as the mcpServers parameter of the session/new ACP request; .mcp.json is only ever read from pack directories, never written. |
| f-42 | Instruction delivery anti-patterns (.mdc, rules/, AGENTS.md) and AGENTS.md/.goosehints as secondary operator mechanism | `crates/buzz-agent/src/hints.rs:63-64` · `crates/buzz-agent/src/hints.rs:27-31` | The positive half is real: the in-repo runtime (buzz-agent) auto-loads AGENTS.md from cwd walking up to the git root plus ~/AGENTS.md, matching 'some agent runtimes auto-load AGENTS.md from $AGENT_CWD (walking up to git root)'. The negative half holds: no cod… |
| f-51 | Missing defaults key in plugin.json: precedence level 4 skipped, fields fall through to built-in defaults | `crates/buzz-persona/src/merge.rs:89-91` · `crates/buzz-persona/src/merge.rs:38-39` | manifest defaults is Option; when absent, resolve_persona_config receives None, substitutes an empty object (level 4 becomes a no-op) and fields land on built-ins: thread_replies=true, broadcast_replies=false, triggers/model/temperature/subscribe → None. Test… |
| f-53 | Operator env var precedence: buzz-acp MUST NOT override env vars already set in the parent environment | `crates/buzz-acp/src/acp.rs:469-471` · `crates/buzz-acp/src/acp.rs:503-511` | AcpClient::spawn takes extra_env (acp.rs 451-455) and checks the parent environment per key before injecting — operator values win, matching PF-6 which the spec marks Implemented. Regression test at acp.rs 2969-2988. The desktop spawn applies the same parent-… |
| f-54 | Deploy-time resolution: defaults merged into per-persona effective config at pack load; defaults object never forwarded… | `crates/buzz-persona/src/pack.rs:119-121` · `crates/buzz-persona/src/pack.rs:410` | load_pack resolves defaults into each LoadedPersona at load time; ResolvedPack/ResolvedPersona expose only the resolved per-persona values (model/temperature/subscribe/triggers/env-var projection) — no defaults object exists on any resolved/runtime-facing typ… |
| f-55 | null values treated as absent, falling through to next precedence level | `crates/buzz-persona/src/merge.rs:66-71` · `crates/buzz-persona/src/merge.rs:43-46` | null persona values fall through to pack defaults for scalars (merge loop), subscribe (explicit null arm merge.rs:116-121) and triggers (null arm merge.rs:147). The complementary rule — empty [] / {} are present and override — is also implemented and tested (… |
| f-56 | Empty array semantics: subscribe: [] is an intentional override meaning subscribe to nothing | `crates/buzz-persona/src/merge.rs:29-32` · `crates/buzz-persona/src/merge.rs:105-125` | Fully implemented with tests: [] blocks pack-default inheritance, null/absent falls through, and triggers:{} similarly overrides to built-ins. Minor caveat: LoadedPersona flattens the resolved Option (pack.rs:436 `resolved.subscribe.unwrap_or_default()`) afte… |
| f-57 | Empty object semantics: triggers: {} overrides pack default entirely; sub-fields fall to built-in defaults | `crates/buzz-persona/src/merge.rs:44-46` · `crates/buzz-persona/src/merge.rs:143-145` | triggers: {} is treated as present (not null), shallow-replaces the pack default, and parse_triggers fills missing sub-fields with built-ins (mentions=true, keywords=[], all_messages=false) — exactly the spec's example. subscribe: [] similarly stays Some(empt… |
| f-58 | Shallow replacement merge semantics: no deep merge; object and array fields replaced whole | `crates/buzz-persona/src/merge.rs:127-131` · `crates/buzz-persona/src/merge.rs:143-145` | Simple fields: first-defined wins (merge_behavioral_config); triggers object replaced whole with missing sub-fields falling to built-in defaults (mentions=true, keywords=[], all_messages=false — parse_triggers); subscribe array replaced whole with []-means-ov… |
| f-60 | Temperature validation: passed through without range validation; buzz pack validate checks type only | `crates/buzz-persona/src/persona.rs:193` · `crates/buzz-persona/src/validate.rs:121-130` | Type checking happens implicitly via serde (Option<f64> in both frontmatter and manifest defaults); no range validation exists anywhere; the value is projected verbatim into GOOSE_TEMPERATURE. Matches the spec exactly. |
| f-63 | Env var mapping: model split on first colon to GOOSE_PROVIDER/GOOSE_MODEL; GOOSE_TEMPERATURE; GOOSE_CONTEXT_LIMIT; omit… | `crates/buzz-persona/src/persona.rs:324-329` · `crates/buzz-persona/src/resolve.rs:369-397` | runtime_env_vars projects from LoadedPersona, which is built from resolve_persona_config output (levels 3-5 already merged) — env vars reflect fully-resolved values as spec 815-817 requires. Omitted model → no GOOSE_PROVIDER/GOOSE_MODEL (guarded by if let Som… |
| f-64 | Per-subprocess env injection via AcpClient::spawn extra_env with std::env::var parent-environment check (PF-6, implemen… | `crates/buzz-acp/src/acp.rs:451-456` · `crates/buzz-acp/src/acp.rs:503-511` | Implemented as specified with two nuances: the parent-env presence check uses std::env::var_os rather than std::env::var (functionally equivalent for the precedence rule), and Config.persona_env_vars is currently populated only with generated CODEX_CONFIG ent… |
| f-66 | buzz pack validate command (schema-validates plugin.json, identity fields, behavioral config; warns on unknown keys/nam… | `crates/buzz-cli/src/lib.rs:1782-1787` · `crates/buzz-cli/src/commands/pack.rs:24` | Full pipeline: structural validation via load_pack (manifest required fields, persona identity fields name/display_name/description non-empty, typed behavioral config), semantic checks (zero personas, duplicate/invalid/overlong names), advisory warnings for u… |
| f-75 | Desktop Agents section import accepts .agent.json or .agent.png snapshots | `desktop/src/features/agents/ui/UnifiedAgentsSection.tsx:135-141` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:36-47` | The Agents UI file input and drop zone accept exactly .agent.json/.agent.png; the backend decodes both (PNG via embedded buzz_agent_snapshot tEXt chunk, JSON directly) with size caps and preview/confirm flow. Matching the spec's surrounding text, legacy perso… |
| f-76 | Desktop Agent teams import accepts .team.json/.team.png (buzz-team-snapshot v1); persona-pack .zip rejected with error | `desktop/src-tauri/src/commands/team_snapshot.rs:33-35` · `desktop/src-tauri/src/commands/team_snapshot.rs:59-63` | decode_team_snapshot_from_bytes accepts PNG (tEXt chunk) and JSON buzz-team-snapshot v1 payloads with size caps, and detects ZIP magic bytes (PK) to reject persona-pack zips with a migration-oriented error, exactly as documented. The Agents-section counterpar… |
| f-77 | Persona packs and desktop snapshots are separate non-interchangeable formats; no conversion command either direction | `desktop/src-tauri/src/commands/personas/snapshot/import.rs:33-34` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:41-43` | Agents import accepts .agent.json/.agent.png; legacy persona files AND .zip are rejected with an error directing users to snapshots (spec's wording says the team import rejects zip — in code the rejection lives in the agent-snapshot import path and the team f… |
| f-78 | buzz pack inspect command (inspection; resolved config usable as reference for manual recreation) | `crates/buzz-cli/src/lib.rs:1788-1791` · `crates/buzz-cli/src/commands/pack.rs:61-63` | Prints the fully resolved (post-merge, post-model-split) per-persona config: model/provider, temperature, max_context_tokens, subscribe, triggers, thread/broadcast replies, MCP server count, skills, avatar, system-prompt preview, and projected env vars — suff… |
| f-83 | Anti-pattern reference table (nonexistent goose flags, .mdc files, root skills/, SSE, parent-process GOOSE_MODEL, defau… | `crates/buzz-agent/src/hints.rs:8` · `crates/buzz-persona/src/merge.rs:127-131` | Documentation reference table; every row checkable in this repo is consistent with the code: root skills/ absent from SKILL_DIRS; defaults merging is shallow replacement (object/array replaced whole, tested); per-subprocess extra_env injection exists as the a… |
| f-84 | Secret management: never embed secrets; ${VAR_NAME} references in env blocks; future refusal to start on unresolved vars | `crates/buzz-persona/src/resolve.rs:276` · `crates/buzz-persona/src/resolve.rs:494-506` | The spec's described current behavior (literal ${VAR_NAME} passthrough, interpolation and refusal-to-start explicitly future) matches the code exactly, including a pinning test. The 'never embed secrets' part is authoring guidance, not enforceable code. |
| f-88 | Backward compatibility: V6 namespaced buzz: block format not supported; only flat top-level fields accepted | `crates/buzz-persona/src/persona.rs:173-175` · `crates/buzz-persona/src/persona.rs:416-421` | Persona frontmatter uses deny_unknown_fields, so a V6 `buzz:` block is a hard parse error; only flat top-level fields deserialize. RawManifest has no `buzz` field — a V6 plugin.json `buzz` block is ignored (validator emits an unknown-key warning per advisory_… |

## [+] Implementado e não documentado no spec (direção reversa)

| id | comportamento | evidência | nota |
|---|---|---|---|
| rev-1 | Desktop imports .agent.json/.agent.png/.team.json/.team.png snapshot files, with content sniffed by magic bytes (never the extension) and p… | `desktop/src-tauri/src/commands/media_download.rs:397-413` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:215-219` | PARTIAL gap: the four accepted file types ARE documented in spec §11 'Desktop App Import' (lines 914-919). What the spec never mentions: sniffing is by PNG magic not filename, the 5/10/25/50 MiB caps, and that a mis-named file still decodes correctly. Belongs… |
| rev-2 | PNG-body-becomes-avatar: on export the agent's avatar is used as the .agent.png image body; on import the PNG image body is adopted as the … | `desktop/src-tauri/src/commands/personas/snapshot/import.rs:242-261` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:252-259` | Spec §11 names `.agent.png` only as a file extension; it never says the image body is the avatar nor that it overrides a manifest avatar URL. Would belong in §11 Desktop App Import. |
| rev-3 | Unknown fields in snapshot JSON (.agent.json/.team.json manifests) are silently dropped by serde, in contrast to persona frontmatter where … | `desktop/src-tauri/src/managed_agents/agent_snapshot.rs:164-166` · `desktop/src-tauri/src/managed_agents/agent_snapshot.rs:287-292` | Zero occurrences of deny_unknown_fields anywhere under desktop/src-tauri, so AgentSnapshot, AgentSnapshotDefinition, TeamSnapshot etc. all accept and drop unknown keys. Spec documents the persona-frontmatter hard-error side (§10) but is silent on snapshot len… |
| rev-4 | Team snapshot import does not deduplicate members: every member in the file is minted as a brand-new agent (1:1 map over snapshot.members),… | `desktop/src-tauri/src/commands/team_snapshot.rs:145-155` · `desktop/src-tauri/src/commands/team_snapshot.rs:497-498` | Spec §11 Desktop App Import says team import 'Accepts .team.json or .team.png' and nothing about member handling; no dedup or merge semantics are documented anywhere in the spec. |
| rev-5 | The persona frontmatter accepts a `runtime:` key (e.g. 'goose', 'buzz-agent', 'claude') that switches the resolved env-var prefix: runtime … | `crates/buzz-persona/src/persona.rs:145-148` · `crates/buzz-persona/src/persona.rs:192` | Because frontmatter is deny_unknown_fields, a spec-faithful author would believe `runtime:` is an invalid key — yet it parses and changes env-var projection. Absent from §4 Field Reference, §10 Canonical Behavioral Config Field Schema, and §10 Env Var Mapping… |
| rev-6 | A second, encrypted snapshot wire format exists: locked agent cards with format discriminator 'buzz-agent-snapshot-encrypted', a NIP-44 v2 … | `desktop/src-tauri/src/managed_agents/agent_snapshot_envelope.rs:1-7` · `desktop/src-tauri/src/managed_agents/agent_snapshot_envelope.rs:35-41` | The spec's snapshot paragraph (§11, lines 910-928) describes snapshots as plain exported files; the encrypted variant, its format string, key-endpoint unlock rule, and refusal semantics are entirely undocumented. |
| rev-7 | Snapshots can bundle agent memory at three levels (none/core/everything) as plaintext entries; on import each entry is re-published as a fr… | `desktop/src-tauri/src/managed_agents/agent_snapshot.rs:67-81` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:445-449` | The word 'memory' does not appear anywhere in PERSONA_PACK_SPEC.md. Team snapshots inherit the same per-member memory machinery (desktop/src-tauri/src/commands/team_snapshot.rs:776-828). Would belong in §11 Desktop App Import. |
| rev-8 | An in-chat discovery/import path exists that the spec never mentions: snapshot attachments in message timelines render as cards with an 'Ad… | `desktop/src/shared/ui/markdown/AgentSnapshotCard.tsx:47-56` · `desktop/src-tauri/src/commands/media_download.rs:433-453` | Spec §11 describes only 'Agents section → Import' / 'Agent teams section → Import' (file-picker paths). The message-card path, its imeta sha256/size verification, and same-relay origin restriction are undocumented. |
| rev-9 | Agent Trading Cards: mint_agent_card generates a collectible card PNG via OpenAI image generation and embeds the buzz_agent_snapshot manife… | `desktop/src-tauri/src/commands/personas/card.rs:1-6` · `desktop/src-tauri/src/commands/personas/card.rs:8-17` | An entire snapshot-producing surface (mint dialog, card viewer, locked minting) absent from the spec; §11 Desktop App Import / Distribution is where it would belong. |
| rev-10 | The agent import path explicitly rejects legacy persona file formats — .persona.md, .persona.json, .persona.png, and .zip — with a migratio… | `desktop/src-tauri/src/commands/personas/snapshot/import.rs:33-46` · `desktop/src-tauri/src/commands/team_snapshot.rs:33-35` | Spec documents only the zip rejection on the TEAM import (§11 line 918). The agent-side rejection of .persona.md/.persona.json/.persona.png (implying retired desktop persona-file import formats) and the flat-team-JSON detection are undocumented. |
| rev-11 | Every snapshot/team import mints a brand-new identity: fresh nostr keypair + NIP-OA auth tag per agent, no source identity material consume… | `desktop/src-tauri/src/commands/personas/snapshot/import.rs:452-454` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:515-518` | Also enforced by the export-side secret-exclusion contract (desktop/src-tauri/src/managed_agents/agent_snapshot.rs:19-41, 'The following fields are NEVER serialized'). None of this appears in the spec. |
| rev-12 | Imports of snapshots containing a respond_to allowlist force a Keep/Clear user decision (default Clear); Clear on allowlist-mode downgrades… | `desktop/src-tauri/src/commands/personas/snapshot/import.rs:139-153` · `desktop/src-tauri/src/commands/personas/snapshot/import.rs:190-194` | The spec's only respond_to mention is the `respond_to`→`triggers` legacy alias (§4/§14) — a different field in the pack format. Snapshot allowlist semantics belong in §11 Desktop App Import. |
| rev-13 | Snapshot attachments can be copied and re-shared through the system clipboard via Buzz-specific HTML carrying a versioned JSON payload in a… | `desktop/src/features/messages/lib/agentSnapshotClipboard.ts:4-8` · `desktop/src/features/messages/lib/agentSnapshotClipboard.ts:43-64` | Another sharing/discovery path for the snapshot formats that the spec's §11 Desktop App Import never mentions. |
