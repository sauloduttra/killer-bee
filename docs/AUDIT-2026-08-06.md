# Auditoria 2026-08-06 — seis dimensões, verificação adversarial

**Método.** 15 agentes em duas fases: 6 leitores paralelos (emissor Python, site,
citações, honestidade, segurança, narrativa/oportunidades), depois um **segundo leitor
adversarial por achado forte** — instruído a REFUTAR, reabrindo arquivo e linha.
~2,0M tokens, 556 chamadas de ferramenta, tudo só-leitura; um único escritor aplicou
os fixes depois (AUTONOMIA §2.6). Regra D-014 em vigor: cada dimensão reportou também
o que **não** conseguiu coletar (51 itens de `not_collected` registrados no resultado
bruto do workflow).

**Placar:** 56 achados únicos · 9 verificados adversarialmente · **9/9 CONFIRMADOS**
(0 refutados) · 11 oportunidades.

---

## Confirmados e CORRIGIDOS neste bloco

| # | Sev. | Onde | O quê | Fix |
|---|---|---|---|---|
| 1 | crítico | `site/.../page.tsx` (6 pontos) | Downloads com `<a href="/downloads/...">` cru — **404 em todo botão** no GitHub Pages de projeto; latente porque o CI nunca rodou | `downloadHref()` com `NEXT_PUBLIC_BASE_PATH` + teste de integridade de links que roda também no build com basePath (provado nos dois modos) |
| 2 | crítico | `packs/.../README.md:50` | "Adversary and Guard react to every patch" — **falso no caminho de install documentado**: agente importado responde só a menção | Seção reescrita: escopo honesto (import = menção; low threshold só via `buzz-acp --subscribe config --config`, caminho não exercitado e dito) |
| 3 | alto | `killerbee/validate.py:54` | `display_name`/`team.name` só de espaços passava no validate e **o app rejeitava** o arquivo emitido | Checagens com `.strip()` espelhando o trim do upstream + testes |
| 4 | alto | `killerbee/cli.py:114` | `team.id` cru como nome de arquivo: `../x` escrevia fora de `dist/`, `a:b` estourava no Windows | Charset validado (`_TEAM_ID`), nomes reservados do Windows rejeitados; restrição documentada como nossa (o d-tag upstream aceita mais) |
| 5 | alto | `pages.yml:50` | `NEXT_PUBLIC_SITE_URL` recebia `origin` sem o path — canonical/og:url **404avam** | `base_url` (origin+path); canonical conferido no build com basePath |
| 6 | alto | `rendered-html.test.mjs` | Nenhum teste resolvia href contra `out/` — o teste que pegaria o 404 não existia | Teste novo percorre todo `.html`, exige basePath e resolve cada link interno |
| 7 | alto | `ProfileMeter.tsx:76` | Hint do threshold prometia "reacts to everything" sem qualificar — classe D-017 | Hint reescrito dizendo onde cada metade vale |
| 8 | alto | `acp_rules.py:58` | Cabeçalho emitido citava `--rules`, **flag que não existe** (é `--config`) | Corrigido + nota; artefatos regenerados |
| 9 | alto | `packs/.../README.md:45` | "The workflow trigger posts..." — workflow que o pack **não contém**, no presente | Reescrito ("you post — the pack does not ship a workflow"); mesma varredura na home do site |

## Do restante, também corrigidos agora (24)

**Emissor:** `channels: []` → erro em vez de virar `all` silencioso · `tags: abc` → erro
em vez de tuple de caracteres · seções YAML nulas → `PackLoadError` legível · `model: 123`
→ erro no load · paridade EXATA do frontmatter com `persona.rs:277-319` (inclusive
limites 1 MiB/256 KiB e corpo byte a byte — o `lstrip` antigo apagava linha em branco
inicial do prompt) · BOM com erro específico · `_toml_string` escapa controle ·
`_SLUG` alinhado à gramática real do relay (sem `.`) · caps de 5/25 MiB aplicados no
build · `fitsIn30178` medido compacto · `generatedFrom` sem caminho absoluto ·
arrays vazias omitidas como o exportador upstream (`Vec::is_empty`) · `pngtext`
endurecido (ValueError nomeado, teto de chunk, para no IEND, Latin-1 com contexto) ·
teste do `gh` virou puro (guarda extraída) · 6 fixtures ganharam o allow-marker
prometido.

**Scanner de segredos:** arquivos `.env`/`.env.*` agora são varridos (o filtro por
`suffix` os pulava) · valor sem aspas estilo dotenv · `ncryptsec`, Stripe live, npm,
GitLab PAT, SendGrid, Google API, `xoxc` · 64-hex nu (com exclusão por contexto de
sha256) · allow-marker antes de qualquer janela · linha longa varrida em janelas
sobrepostas em vez de truncada · flag `--fail-on media` para o CI endurecer quando
quiser.

**Site:** `aria-pressed` nos filtros (+ traço de estado em forced-colors) · SVG do hero
sem `role="img"` indevido · `.syn` e números de linha em tinta que passa 1.4.3 ·
hanging indent real (o `text-indent: hanging` era inválido e não fazia nada) · âncoras
de linha fora da ordem de Tab · tokens mortos removidos (`--wash`/`--hair`/`--ramp-2`) ·
og:site_name/og:type preservados nas páginas internas · copy da home alinhada ao botão
(issue, não PR) · comentário falso de animação corrigido · fontes re-buscadas com filtro
de subset real (335→232 KB versionados) · **og:image** (gerada por `scripts/make-og.mjs`,
com o favo e o crédito), **favicon**, **robots.txt**, **sitemap.xml**.

**CI/workflows:** todas as actions pinadas por SHA (resolvidos ao vivo via
`gh api .../commits/<tag>` em 2026-08-06) · `pages: write`/`id-token: write` só no job
deploy · Dependabot para actions e npm · teste de export roda TAMBÉM com
`PAGES_BASE_PATH` no pages.yml · `@eslint/eslintrc` removido (não usado).

**Novidades de produto (oportunidades S implementadas):**

- **`killerbee inspect <arquivo>`** — abre `.agent.json/.png` e `.team.json/.png`
  (nosso ou de terceiro) e mostra prompt, model, provider, timeouts e sha256 ANTES do
  import. A tese "leia antes de rodar" em forma de comando.
- **sha256 + tamanho por artefato** no `catalog.json` e na página (componente
  `Checksums`), com teste que confere o hash publicado contra o arquivo servido. É o
  `x` que o card de import por chat do Buzz exige (`markdownFileCard.ts:101-103`) — a
  peça que faltava para o pack circular como card importável no Nostr.
- **Equação do README corrigida** (ver abaixo) + teste de propriedade novo: a
  compilação do perfil é injetiva salvo `propagation`, e `low` ≠ `medium` na regra ACP.

---

## Ainda abertos, por prioridade

**Média (fazer no próximo bloco):**
- Página/manifesto `SECURITY.md` + `docs/PACK-REVIEW.md` — modelo de ameaça de pack
  malicioso (prompt de terceiro rodando com credencial do usuário; `.agent.png` parece
  imagem e instala agente). O produto pede governança demonstrável.
- Scaffold de contribuidor: `packs/TEMPLATE/`, JSON Schema do `killerbee.yaml`,
  ISSUE/PR templates (efetivos só com remote público).
- Sumário executivo em inglês no topo de `PROTOCOL-NOTES.md` — o doc que prova o rigor
  está em pt-BR e é para onde README e site mandam o leitor.
- CSP via `<meta>` entra depois dos primeiros loads e `frame-ancestors` é inerte —
  limite do GitHub Pages; documentado, teste trava a presença. Resolver = host com
  headers (decisão de infra, junto com o remote).

**Baixa (oportunista):**
- Fade da régua de filtros esmaece o último chip mesmo sem overflow (pede JS ou
  scroll-driven animation).
- `.agent.png` distribuído é 1×1 transparente; o card de chat do Buzz usa o próprio
  arquivo como thumbnail — rasterizar a assinatura de dança dentro do PNG (puro,
  sem Pillow) transformaria o instalável em cartão auto-explicativo.
- Detector de entropia de Shannon no scanner (as regras atuais são por padrão; base64
  está declaradamente fora do alcance).
- `email`/`private_ip_url` baixa geram ruído em docs — avaliar allowlist por caminho.

## Oportunidades maiores (com dependência)

1. **Vídeo de 90s sem credencial** — o item 15 do DoD está bloqueado por escolha de
   roteiro, não por técnica. Roteiro proposto pela auditoria, todo com material que já
   existe: 0-10s tese + contador de bytes → 10-25s prompt verbatim (`##` na tela,
   `#L12`) → 25-40s download + 4 cliques contados → 40-55s team reconstituindo 3
   membros e o app rotulando "Mixed models" → 55-70s o corte honesto (STOPPED, falta
   credencial) → 70-85s o que NÃO existe (sem deep link, sem `buzz install`, menção
   não garante resposta) → 85-90s repo, Apache-2.0, não afiliado, **por Saulo Duttra**.
   O trecho "3 agentes respondendo" fica DELIBERADAMENTE fora do corte.
2. **`imeta` pronto por artefato** — com o sha256 já no catálogo, falta emitir o bloco
   `["imeta","url …","m …","x <sha256>","size …","filename …"]` por pack (forma exata
   no e2e do upstream). Upload real ao Blossom continua 🔴 credencial.
3. **Emissor L3 offline** — schema da projeção de membro do 30178 (não existe no
   upstream; a NIP-AP delega ao cliente) + `killerbee event` emitindo o evento **não
   assinado** + fixtures. Contribuição real ao ecossistema, sem tocar em chave.
4. **Metodologia quantitativa** — a seção do README foi corrigida nesta rodada (Hill,
   `n` definido, "não computada em lugar nenhum" dito com todas as letras). Falta a
   seção "o que falsificaria isto" com uma medida de resultado, e justificar/sensibilizar
   os pares de timeout (300/600, 900/1800, 3600/7200), que são escolha nossa.

## O que a auditoria NÃO cobriu (recorte honesto)

CVEs pós-cutoff das dependências (exigiria OSV/npm audit com rede dedicada); gitleaks
real (binário ausente — só o scanner próprio rodou); headers reais do GitHub Pages
(site não publicado); PNG hostil contra o decoder DO UPSTREAM (só o nosso lado foi
endurecido); Lighthouse/CLS em navegador. Lista integral de `not_collected` no
resultado do workflow (`wf_45d7f146-e4c`).
